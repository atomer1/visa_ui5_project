<core:FragmentDefinition xmlns:tnt="sap.tnt"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form">

    <VBox class="sapUiSmallMargin">
        <f:SimpleForm id="simpleFormSupplierTables" editable="true" layout="ResponsiveGridLayout">
            <Toolbar >
                <Title class="addColor titleSize sapUiSmallMargin" level="H2" text="{i18n>yourSuppliers}"></Title>
            </Toolbar>
            <f:content>

                <Table id="idSupplierTable" inset="false" items="{paymentStatusModel>/suppliersData}">
                    <headerToolbar class="toolbarHeader">
                        <Toolbar>
                            <SearchField search=".onFilterPosts" />
                        </Toolbar>
                    </headerToolbar>
                    <columns>
                        <Column>
                            <Text text="{i18n>companyName}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>UBOName}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>BankAccNum}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>email}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>sapCustomer}" />
                        </Column>
                        <Column>
                            <Text text="{i18n>paymentMethod}" />
                        </Column>
                    </columns>
                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{paymentStatusModel>companyName}" />
                                <Text text="{paymentStatusModel>uboName}" />
                                <Text text="{paymentStatusModel>bankAccountNum}" />
                                <Text text="{paymentStatusModel>email}" />
                                <Text text="{paymentStatusModel>sapCustomer}" />
                                <ObjectStatus state="{path:'paymentStatusModel>status', formatter:'.formatter.fnChangeColorPaymentMethod'}" text="{paymentStatusModel>paymentMethod}" />
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>
            </f:content>
        </f:SimpleForm>
    </VBox>
</core:FragmentDefinition>